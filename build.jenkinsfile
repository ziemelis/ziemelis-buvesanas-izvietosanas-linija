def name = credentials('test')
pipeline {
    agent any
    stages {
        stage("Hello") {
            steps {
                sh 'aws sts get-caller-identity'
            }
        }
    }
}
